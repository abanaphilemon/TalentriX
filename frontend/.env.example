# Backend API URL.
# - Local dev: keep this pointing at your local backend.
# - Leave it UNSET for Vercel: the app falls back to a relative "/api" path,
#   which vercel.json proxies to your Render backend (no CORS needed).
# - Or set it directly to your Render backend: https://your-app.onrender.com/api
VITE_API_URL=http://localhost:5000/api

# Google OAuth — required for the "Get Started" Google sign-in button.
# 1. Create a project + OAuth Web Client ID at https://console.cloud.google.com/apis/credentials
# 2. Add http://localhost:5173 to "Authorized JavaScript origins"
# 3. Paste the Client ID below and restart `npm run dev`
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com